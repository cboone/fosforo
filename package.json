{
  "name": "fosforo-markdown-tooling",
  "version": "0.0.0",
  "private": true,
  "description": "Pins the two Node tools that format and verify this repository's Markdown. Not a package, not published, and not part of any build: `zig build` never reads this file.",
  "license": "MIT",
  "devDependencies": {
    "markdownlint-cli2": "0.23.2",
    "prettier": "3.9.6"
  },
  "scripts": {
    "format": "prettier --write \"**/*.md\"",
    "format:check": "prettier --check \"**/*.md\"",
    "lint:md": "markdownlint-cli2"
  }
}
